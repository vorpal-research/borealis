Note:
Cfgparser depends on dbglog (http://sourceforge.net/projects/dbglog). If you
want build cfgparser install dbglog first.

How to install:

1) ./bootstrap.sh

2) ./configure

3) make
If you want build unit tests too (they will not be installed):
   make test

4) make install
